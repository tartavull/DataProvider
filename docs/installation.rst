.. highlight:: shell

============
Installation
============


Stable release
--------------

To install DataProvider, run this command in your terminal:

.. code-block:: console

    $ pip install dataprovider

This is the preferred method to install DataProvider, as it will always install the most recent stable release. 

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for DataProvider can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/tartavull/dataprovider

Or download the `tarball`_:

.. code-block:: console

    $ curl  -OL https://github.com/tartavull/dataprovider/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/tartavull/dataprovider
.. _tarball: https://github.com/tartavull/dataprovider/tarball/master
